pip install --upgrade flask jinja2  

python -c "import jinja2; print(jinja2.__version__)" 

python -c "import flask; print(flask.__version__); print(flask.__file__)"

findstr /n "escape" venv\Lib\site-packages\flask\__init__.py

pip show scikit-learn

deactivate


Get-Process python,pythonw -ErrorAction SilentlyContinue | Stop-Process -Force; Remove-Item -Recurse -Force .\venv -ErrorAction SilentlyContinue; if (!(Test-Path .\venv)) {"venv deleted successfully"} else {"venv deletion failed"}

python --version


py -3.11 -m venv venv


.\venv\Scripts\activate


python -m pip install --upgrade pip


pip install -r requirements.txt






if (Test-Path .\venv) { Remove-Item -Recurse -Force .\venv; if (!(Test-Path .\venv)) { "venv deleted successfully" } else { "venv deletion failed" } } else { "venv not found, already deleted" }




py -3.11 -m venv sklearn-old
.\sklearn-old\Scripts\activate


====================================================================================

1) Remove old incompatible packages

pip uninstall -y Flask Jinja2 Werkzeug MarkupSafe itsdangerous gunicorn scikit-learn numpy scipy matplotlib pandas


2) Verify they are removed

pip freeze | findstr "Flask Jinja2 Werkzeug MarkupSafe itsdangerous scikit-learn numpy scipy pandas matplotlib"

3) Install Python 3.11 compatible versions

pip install Flask==3.1.3 Jinja2==3.1.6 Werkzeug==3.1.3 MarkupSafe==3.0.2 itsdangerous==2.2.0 gunicorn==23.0.0 numpy==1.26.4 scipy==1.11.4 pandas==2.1.4 matplotlib==3.8.2 scikit-learn==1.3.2

4) Verify installed versions

python -c "import flask,jinja2,sklearn,numpy,pandas,scipy; print('Flask',flask.__version__); print('Jinja2',jinja2.__version__); print('sklearn',sklearn.__version__); print('numpy',numpy.__version__); print('pandas',pandas.__version__); print('scipy',scipy.__version__)"

5) Run your app

python app.py

One important note: after this, your next possible error will likely be the pickle model:

ModuleNotFoundError: No module named 'sklearn.ensemble._gb_losses'

because your .pkl files were created with an older scikit-learn. If that appears again, we will handle the model compatibility separately.


pip uninstall -y scikit-learn
pip install scikit-learn==0.22.2.post1

Verify:
python -c "import sklearn; print(sklearn.__version__)"

pip install scikit-learn==0.22.2.post1
python -c "import sklearn; print(sklearn.__version__)"


deactivate

Remove-Item -Recurse -Force .\venv


if (!(Test-Path .\venv)) {"venv deleted"} else {"venv still exists"}

Get-Process python,pythonw -ErrorAction SilentlyContinue | Stop-Process -Force; Remove-Item -Recurse -Force .\venv -ErrorAction SilentlyContinue; if (!(Test-Path .\venv)) {"venv deleted"} else {"venv still exists"}


# ✅✅✅✅✅
Get-ChildItem .\venv -Recurse | Unblock-File; attrib -R -S -H .\venv /S /D; Remove-Item .\venv -Recurse -Force; if (!(Test-Path .\venv)) {"venv deleted"} else {"venv still exists"}


venv is deleted successfully. ✅

Now create the old-compatible environment for your pickle model.

First check if Python 3.8 is installed:

Check your available versions:
py -0p

py -3.11 -m venv venv

.\venv\Scripts\activate

python --version

py -op


deactivate

py -0p

Now create the project environment using Python 3.8:
py -3.8 -m venv venv

Activate it:
.\venv\Scripts\activate

Verify the active Python:
python --version

First check if it exists:
Test-Path .\requirements.txt

If it returns True, delete it:
Remove-Item .\requirements.txt -Force

Verify:
if (!(Test-Path .\requirements.txt)) {"requirements.txt deleted"} else {"requirements.txt exists"}

2) Install your project dependencies
Run:
python -m pip install --upgrade pip setuptools wheel


Next step 1: Install the legacy packages

Run:
pip install Flask==1.1.1 Jinja2==2.10.1 Werkzeug==0.15.5 itsdangerous==1.1.0 scikit-learn==0.22.2.post1 pandas numpy scipy gunicorn==19.9.0

Wait for completion.

python.exe -m pip install --upgrade pip

Next step 2: Verify versions
Run:
python -c "import sklearn, flask, jinja2; print('sklearn', sklearn.__version__); print('flask', flask.__version__); print('jinja2', jinja2.__version__)"

pip uninstall -y MarkupSafe
pip install MarkupSafe==1.1.1

Then verify again: ✅✅✅✅✅
python -c "import sklearn, flask, jinja2, markupsafe; print('sklearn', sklearn.__version__); print('flask', flask.__version__); print('jinja2', jinja2.__version__); print('markupsafe', markupsafe.__version__)"

This should be compatible with your old .pkl models.

Next: run your Flask app
Execute:
python app.py

# ✅✅✅✅✅
pip uninstall -y numpy
pip install numpy==1.19.5

python app.py













